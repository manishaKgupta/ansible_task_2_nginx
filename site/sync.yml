- hosts: web
  tasks:
  - name: "sync website"
    synchronize:
      src: /home/ubuntu/ansible/site/
      dest: /var/www/{{ domain }}
      archive: no
      checksum: yes
      recursive: yes
      delete: yes
    become: no
